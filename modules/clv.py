import pandas as pd
from db import clv_execute_get_cogs

def run(o_df,s_df):

    # Get the outcome and survival data as pandas df 

    # Pull in the cogs data with the tdsp

    # Apply the Math

    print("Ran the CLV\n\n")

    print(o_df,s_df)